version: 0.0
os: linux
files:
#  - source: /scripts/vhosts.conf
#    destination: /etc/nginx/conf.d/
  - source: /
    destination: /opt/alfresco-community/tomcat/webapps/Bachmans
hooks:
  AfterInstall:
    - location: scripts/after.sh
      timeout: 600
      runas: root
  ApplicationStart:
    - location: scripts/start.sh
      timeout: 300
      runas: root
  ValidateService:
    - location: scripts/validate.sh   
      timeout: 300
      runas: root
